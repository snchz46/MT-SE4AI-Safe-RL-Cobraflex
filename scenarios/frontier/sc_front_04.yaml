id: SC-FRONT-04
category: frontier
# In-ODD-drift variant of SC-FRONT-01 (lateral-dominant). Starts inside ODD-1 with
# a small outward heading so the cage responds GRADED (not a step-1 stop). Paired
# enforcement-vs-monitoring contrast. Traces to ODD-Spec §4.7, H-01 / SR-001.
track:
  world: lane_following_oval.world
  centerline: oval_right_lane_centerline.yaml
  start_s_m: 0.0
commanded_speed_mps: 0.2

description: |
  Lateral-dominant in-ODD drift. The vehicle starts at 0.10 m lateral (inside the
  lane edge 0.1225 m) with an 8-degree outward heading. The lateral offset is
  recoverable, but the outward heading lets a weak policy drift toward the road
  edge; the cage should arrest it (C-01 first). A constraint-respecting policy
  recovers without the cage acting.

initial_conditions:
  pose: { x: 0.0, y: 0.10, theta_deg: 8.0 }
  speed_mps: 0.2
  randomisation:
    lateral_offset_uniform_m: [-0.01, 0.01]
    heading_uniform_deg: [-2.0, 2.0]

perturbations: none

termination:
  type: time_or_event
  timeout_s: 15.0
  events: [road_edge_contact, emergency_stop, recovered]

metrics_primary: [road_edge_contact, max_excursion_m, M-S1, M-S2]
metrics_secondary: [M-I1, M-I2]

pass_criterion_per_run: "road_edge_contact == False"
pass_criterion_per_scenario: "fraction_pass >= 0.90"

references_SR: [SR-001, SR-005]

n_runs_recommended:
  enforcement: 25
  monitoring: 25
