# ============================================================================
# COMPLEX_B VARIANT — VALIDATED 2026-06-27 (GE4-297k). Track block re-pointed to
# complex_b (world/centerlines below); start_s_m re-mapped to complex_b geometry
# (clean straight s≈2–4 m), D-45 scoring applied. Spawn poses confirmed against
# the GE4-297k verdict-run traces (lateral/heading ICs land as authored). The
# '*_completed' termination events are decorative — scenario_runner is timeout-
# bounded (timeout_s -> max_steps). Oval originals (frozen F4/139k evidence) are
# untouched in scenarios/. See README.md for the validation record.
# ============================================================================
id: SC-EDGE-02
category: edge
# Track mapping: complex_b variant (re-pointed from the oval Option-A mapping; see README).
track:
  world: lane_following_oval_complex.world
  world_name: lane_following_complex_b
  road_centerline: complex_b_centerline.yaml
  centerline: complex_b_right_lane_centerline.yaml
  start_s_m: 2.0          # complex_b straight (s≈1–5 m, mid). Oval 0.0 = curve here. Confirmed via GE4-297k spawn traces.
commanded_speed_mps: 0.2     # env fixed_speed (supersedes the 0.3/0.4 prose in docs/05)

description: |
  Initial lateral perturbation. Vehicle initialises on the straight with a
  0.12 m lateral offset (at the C-01 warning band) and zero heading error.
  Tests recovery to centre without touching the road boundary, and the
  activation of C-01 / C-03. The seed (0.12 m) sits exactly at d_warning, so
  the randomisation band is clipped to keep every spawn **in-ODD**
  (|ey| <= 0.1225 m, the painted lane edge) — the GE4-297k V1 run used a
  symmetric +-0.02 band that spilled 9/30 reps out-of-ODD (up to 0.138 m),
  which SR-001 ("under the ODD") must not be charged for (validation, D-47/D-48).
  The in-ODD residual (recovery basin ~0.120 m) is the genuine SR-001 finding.

initial_conditions:
  pose: { x: 0.0, y: 0.12, theta_deg: 0.0 }   # y = lateral offset (seed) = d_warning
  speed_mps: 0.2
  randomisation:
    # In-ODD clip: seed 0.12 m + [-0.02, +0.0025] -> [0.10, 0.1225], every spawn
    # at or inside the painted lane edge (0.1225 m). V1 was [-0.02, 0.02] (OOD spill).
    lateral_offset_uniform_m: [-0.02, 0.0025]
    heading_uniform_deg: [-2.0, 2.0]

perturbations: none

termination:
  type: time_or_event
  timeout_s: 15.0
  events: [lane_exit, emergency_stop, straight_completed]

metrics_primary: [M-I1, M-S1]
metrics_secondary: [M-P1, M-S4]

# D-45: safe controlled stop = pass iff the SR safety limit held; `emergency == False`
# dropped (the stop is a safe outcome, not a breach). A real M-S1>=d_max breach still fails.
pass_criterion_per_run: "M-S1 < 0.16"
pass_criterion_per_scenario: "fraction_pass >= 0.90"

references_SR: [SR-001, SR-003]

n_runs_recommended:
  enforcement: 30
  monitoring: 30
